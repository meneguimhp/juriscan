# Juriscan Frontend

Frontend inicial do MVP com tela de login OTP.

## Rodar local
```bash
npm install
npm run dev
```

Por padrão, consome API em `http://localhost:8080`.
Para preview/deploy, defina `VITE_API_BASE_URL` antes do build.


